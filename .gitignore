# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
# Usually these files are written by a python script from a template
# before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot
*.log

# Django stuff:
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
# According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
# Pipfile

# PEP 582; used by PDM, PEP 582 proposal
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
venv* # Bao gồm venv311 hoặc tên tương tự
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# MicrosoftVisualStudioCode configuration
.vscode/

# PyCharm Project files
.idea/

# Sublime Text files
*.sublime-project
*.sublime-workspace

# Các file hệ điều hành
.DS_Store
Thumbs.db

# Các file tạm thời
*.tmp
*~
*.swp
*.swo

# --- Project Specific ---

# 1. Dữ liệu (RẤT QUAN TRỌNG)
# Thư mục data thường chứa file CSV lớn. KHÔNG nên commit dữ liệu lớn vào Git.
# Chỉ commit mã nguồn để tái tạo dữ liệu hoặc hướng dẫn cách tải dữ liệu.
data/
# Nếu bạn CÓ một file dữ liệu NHỎ và QUAN TRỌNG cần commit (ví dụ file config nhỏ),
# bạn có thể bỏ qua nó bằng cách thêm dòng: !data/your_small_file.csv
# Nhưng nói chung là nên bỏ qua toàn bộ thư mục data.

# 2. Model đã lưu (RẤT QUAN TRỌNG)
# Thư mục chứa model đã huấn luyện (vd: pytorch_model.bin) thường RẤT LỚN.
# Model này có thể được tạo lại bằng cách chạy train.py.
saved_model/

# 3. Các file trực quan hóa được tạo ra
# Các file ảnh (.png), báo cáo (.txt) được tạo bởi evaluate.py không cần thiết trong repo.
visualizations/

# 4. Các file cấu hình nhạy cảm (QUAN TRỌNG)
# **TUYỆT ĐỐI KHÔNG** commit API keys, mật khẩu, hoặc thông tin nhạy cảm khác.
# Sử dụng biến môi trường hoặc các cơ chế quản lý cấu hình an toàn khác.
*.envrc
.env
secrets.yaml
secrets.json
config.prod.py
# Thêm bất kỳ file nào bạn dùng để chứa thông tin nhạy cảm ở đây
